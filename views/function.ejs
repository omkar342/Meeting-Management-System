

<% handleClick = function(){
    const toggleButton = document.getElement(".menu");
      
    const navList = document.getElement(".nav-list");

    toggleButton.addEventListener("click",() => {
    
});
}
%>